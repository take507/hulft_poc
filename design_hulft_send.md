# 設計書：hulft_send.sh

**スクリプト名:** `hulft_send.sh`  
**概要:** HULFTファイル送信ラッパースクリプト  
**作成日:** 2026-05-29

---

## 1. 目的

HULFTの送信コマンド（`utlsend`）を呼び出し、ファイル送信を行うラッパースクリプト。
同期モードでは送信後にステータスを確認し、リトライ対象コードが返った場合は自動でリトライを行う。

---

## 2. 実行方法

```bash
bash hulft_send.sh [オプション]
```

### オプション一覧

| オプション | 必須 | 説明 |
|-----------|------|------|
| `-s`, `--system-id <ID>` | ◎ | システムID |
| `-f`, `--hulft-id <ID>` | ◎ | HULFTID |
| `--file <PATH>` | ◎ | 送信対象ファイルのパス |
| `--sync` | | 同期モード（デフォルト） |
| `--async` | | 非同期モード |
| `-w`, `--timeout <SEC>` | | 同期タイムアウト秒（省略時は設定ファイルの値） |

### 実行例

```bash
# 同期送信（タイムアウト120秒）
bash hulft_send.sh -s SYS01 -f HULFTID01 --file /data/send.dat --sync -w 120

# 非同期送信
bash hulft_send.sh -s SYS01 -f HULFTID01 --file /data/send.dat --async
```

---

## 3. 設定ファイル

**ファイルパス:** `hulft_send.conf`（スクリプトと同一ディレクトリ）

| 設定項目 | 説明 |
|---------|------|
| `DEFAULT_TIMEOUT` | 同期タイムアウトのデフォルト値（秒） |
| `RETRY_COUNT` | 最大リトライ回数 |
| `RETRY_INTERVAL` | リトライ間隔（秒） |
| `RETRY_COMPLETE_CODES` | リトライ対象の完了コード（スペース区切り） |
| `RETRY_DETAIL_CODES` | リトライ対象の詳細コード（スペース区切り） |

---

## 4. 業務フロー

### 4.1 全体フロー

```
スクリプト起動
    │
    ▼
引数なし？ ──Yes──▶ ヘルプ表示して終了 (rc=1)
    │ No
    ▼
引数解析 ─────────▶ 不明オプション？ → エラー終了 (rc=1)
    │
    ▼
設定ファイル読み込み ──▶ ファイル不在 or 必須項目不足？ → エラー終了 (rc=2)
    │
    ▼
デフォルト値適用（タイムアウト未指定の場合、設定値を使用）
    │
    ▼
パラメータ検証 ───────▶ 必須項目欠け / ファイル不在 / タイムアウト非数値？ → エラー終了 (rc=1)
    │
    ▼
送信処理（リトライループ）
    │
    ▼
処理終了
```

### 4.2 送信処理（リトライループ）詳細

```
送信試行開始（最大: 初回 + RETRY_COUNT 回）
    │
    ▼
utlsend 実行
    │
    ├─▶ 失敗（非ゼロ終了） ─▶ リトライ上限到達？
    │                              │ Yes ──▶ エラー終了 (rc=5)
    │                              │ No  ──▶ RETRY_INTERVAL 秒待機 → 再試行
    │
    ▼ 成功（終了コード 0）
    │
    ├─ 非同期モード ──▶ 送信要求完了として正常終了 (rc=0)
    │
    └─ 同期モード
            │
            ▼
        utllist でステータス取得
            │
            ├─▶ utllist 失敗 ──▶ エラー終了 (rc=4)
            │
            ▼ 取得成功
            │
        リトライ対象コード？（完了コード or 詳細コードを判定）
            │
            ├─ No（正常コード）──▶ 配信成功として正常終了 (rc=0)
            │
            └─ Yes（リトライ対象）
                    │
                    ├─ リトライ上限到達？ Yes ──▶ エラー終了 (rc=5)
                    └─ No ──▶ RETRY_INTERVAL 秒待機 → 再試行
```

### 4.3 同期モードと非同期モードの違い

| 項目 | 同期モード | 非同期モード |
|------|-----------|------------|
| `utlsend` オプション | `-sync -w <タイムアウト>` 付き | なし |
| 送信完了の確認 | `utllist` でステータスを取得して確認 | 行わない（送信要求の受付完了で終了） |
| リトライ判定 | ステータスコードに基づき判定 | なし |
| タイムアウト指定 | 有効 | 無効 |

---

## 5. ステータスコード判定

### 5.1 HULFTステータスフォーマット

`utllist` の出力 STATUS 列は `完了コード-詳細コード`（例: `0000-0000`）の形式。

### 5.2 リトライ判定ロジック

下記のいずれかに該当する場合、リトライ対象と判定する。

- 完了コードが `RETRY_COMPLETE_CODES` に含まれる
- 詳細コードが `RETRY_DETAIL_CODES` に含まれる

いずれにも該当しない場合は正常完了（または回復不可能エラー）とみなしてリトライしない。

---

## 6. 終了コード一覧

| 終了コード | 定数名 | 意味 |
|-----------|--------|------|
| `0` | `EXIT_OK` | 正常終了 |
| `1` | `EXIT_ERR_PARAM` | パラメータ不正（必須項目欠け・形式エラー等） |
| `2` | `EXIT_ERR_CONFIG` | 設定ファイルの読み込み失敗 |
| `3` | `EXIT_ERR_UTLSEND` | `utlsend` コマンドの失敗 |
| `4` | `EXIT_ERR_UTLLIST` | `utllist` コマンドの失敗またはステータス解析失敗 |
| `5` | `EXIT_ERR_RETRY_OVER` | リトライ上限超過 |

---

## 7. ログ出力

全ログは **標準エラー出力（stderr）** へ出力する。

| レベル | 出力形式 | 用途 |
|--------|---------|------|
| `[INFO]` | `[INFO]  YYYY-MM-DD HH:MM:SS <メッセージ>` | 処理進捗・正常系の状態通知 |
| `[WARN]` | `[WARN]  YYYY-MM-DD HH:MM:SS <メッセージ>` | 非致命的な異常（リトライ発生等） |
| `[ERROR]` | `[ERROR] YYYY-MM-DD HH:MM:SS <メッセージ>` | 致命的エラー（処理中断） |

---

## 8. 関数一覧

| 関数名 | 役割 |
|--------|------|
| `main` | エントリポイント。各処理を順に呼び出す |
| `parse_params` | コマンドライン引数を解析してグローバル変数にセット |
| `validate_params` | 引数の必須チェック・形式チェック |
| `load_config` | 設定ファイルを読み込み必須項目を確認 |
| `apply_defaults` | 未指定項目に設定ファイルのデフォルト値を適用 |
| `run_send_with_retry` | リトライループを制御し送信処理全体を管理 |
| `invoke_utlsend` | `utlsend` コマンドを組み立てて実行 |
| `get_delivery_status` | `utllist` を実行してステータスを取得 |
| `parse_utllist_output` | `utllist` 出力から完了コード・詳細コードを抽出 |
| `should_retry` | ステータスコードがリトライ対象かを判定 |
| `usage` | ヘルプメッセージを表示 |
